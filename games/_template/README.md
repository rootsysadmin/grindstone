# New game template

Filled out properly in Stage 11 (new-game onboarding docs) once `docs/adding-a-game.md` exists to validate against. For now this just reserves the directory.

Whatever lands here must mirror `games/neverness-to-everness/`'s layout, including its own `CLAUDE.md` and `progress.md` — every game directory is a self-contained context unit (see root `CLAUDE.md`'s "Context isolation" note). No game-specific facts belong in root `docs/`.
